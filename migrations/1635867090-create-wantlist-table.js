// This will create the table
exports.up = async function up(sql) {
  console.log('Creating wantlist table...');
  await sql`
    CREATE TABLE wantlist (
      id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      user_id INTEGER REFERENCES users(id),
      user_exchange_item_id INTEGER REFERENCES items(id),
			item_user_id INTEGER,
			item_id INTEGER REFERENCES items(id)
		)
  `;
};

// This will remove the table again
exports.down = async function down(sql) {
  console.log('Dropping wantlist table...');
  await sql`DROP TABLE wantlist`;
};
